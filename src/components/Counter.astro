---

---
<div class="flex gap-2 items-center ">
  <span class="text-5xl lg:text-6xl xl:text-7xl font-bold font-[oswald] text-[#ffce00]">+</span>
  <div
  class="counter text-5xl lg:text-6xl xl:text-7xl font-bold font-[oswald] text-[#ffce00] w-auto md:w-[150px] lg:w-[200px]"
  id="counter"
>0
</div>
</div>
<script>
  const objetivo = 20000;
  const duracion = 3000; // 5 segundos
  const intervalo = 10;  // Actualización cada 10 ms
  const incremento = (objetivo / duracion) * intervalo;

  let contador = 0;
  const elemento = document.getElementById('counter');

  const actualizarContador = setInterval(() => {
      contador += incremento;
      if (contador >= objetivo) {
          contador = objetivo;
          clearInterval(actualizarContador);
      }
      elemento.textContent = Math.floor(contador).toString();
  }, intervalo);
</script>
