---
interface Props {
  title: string;
  address: string;
  phone: string;
}

const { title, address, phone } = Astro.props;
---

<div class="flex flex-col mr-[3rem] mb-2">
  <div class="mb-6">
    <h2
      class="text-xl font-bold text-[#ffce00] animate-fade-down animate-duration-[400ms] font-[oswald]"
    ><span class="text-[#ffce00] text-3xl mr-2">»</span>
      {title}
    </h2>
  </div>
  <p
    class="text-[#fff] mb-2 animate-fade animate-duration-300 animate-delay-[500ms] font-semibold"
  >
    {address}
  </p>
  <p
    class="text-[#fff] mb-2 animate-fade animate-duration-300 animate-delay-[500ms] font-semibold"
  >
    Tel: {phone}
  </p>
</div>
