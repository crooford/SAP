---
interface Props {
  title: string;
  address: string;
  phone: string;
  urlMaps: string;
}

const { title, address, phone, urlMaps } = Astro.props;
---

<div class="flex flex-col mr-[3rem] mb-2 w-[300px]">
  <div class="mb-4 flex">
    <span class="text-[#ffce00] text-3xl mr-2">»</span>
      <h2
      class="text-xl font-semibold text-[#ffce00] animate-fade-down animate-duration-[400ms] font-[oswald] self-center"
    >
    <a href={urlMaps} target="_blank" class="hover:underline ">{title}</a>
    </h2>
  
  </div>
  <p
    class="text-[#fff] mb-2 animate-fade animate-duration-300 animate-delay-[500ms] text-lg font-semibold ml-7 "
  >
    {address}
  </p>
  <p
    class="text-[#fff] mb-2 animate-fade animate-duration-300 animate-delay-[500ms] ml-7"
  >
    <span class="font-semibold">Tel :</span> {phone}
  </p>
</div>
